import { Routes } from '@angular/router';
import { ProfileFormComponent } from '../components/profile-form/profile-form.component';

export const profileRoutes: Routes = [{ path: '', component: ProfileFormComponent }];
